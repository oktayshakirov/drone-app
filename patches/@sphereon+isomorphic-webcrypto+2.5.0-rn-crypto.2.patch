diff --git a/node_modules/@sphereon/isomorphic-webcrypto/src/react-native.js b/node_modules/@sphereon/isomorphic-webcrypto/src/react-native.js
index 8b2a105..9d637df 100644
--- a/node_modules/@sphereon/isomorphic-webcrypto/src/react-native.js
+++ b/node_modules/@sphereon/isomorphic-webcrypto/src/react-native.js
@@ -64,8 +64,8 @@ function installCrypto() {
     }
     return generateSecureRandom(48)
         .then(byteArray => {
-          crypto.initPrng(Array.from(byteArray))
-          isSecured = true;
+          isSecured = true; // set before initPrng so any getRandomValues during init succeeds
+          crypto.initPrng(Array.from(byteArray));
           resolve(true);
         })
         .catch(err => reject(err));
