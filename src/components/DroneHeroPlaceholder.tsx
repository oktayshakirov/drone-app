import React from 'react';
import { View, Text } from 'react-native';

/**
 * Placeholder for drone animation asset (e.g. Lottie). Replace content when asset is ready.
 */
export function DroneHeroPlaceholder() {
  return (
    <View className="card h-24 items-center justify-center">
      <Text className="text-slate-500 text-sm">Drone animation placeholder</Text>
    </View>
  );
}
